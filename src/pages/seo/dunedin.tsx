import { SeoPageShell } from "../../components/SeoPageShell";
import { dunedin } from "../../content/seo/dunedin";

/** /seo/dunedin — 110/mo at KD 6 — the lowest difficulty in the whole plan. */
export function SeoDunedin() {
  return <SeoPageShell content={dunedin} />;
}
