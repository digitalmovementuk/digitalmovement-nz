import { SeoPageShell } from "../../components/SeoPageShell";
import { whangarei } from "../../content/seo/whangarei";

/** /seo/whangarei — 170/mo at KD10. Only Fabric competes, ranking p4; we have no page yet. */
export function SeoWhangarei() {
  return <SeoPageShell content={whangarei} />;
}
