import { SeoPageShell } from "../../components/SeoPageShell";
import { auckland } from "../../content/seo/auckland";

/** /seo/auckland — 1300/mo at KD 45 — year-two target, built now to start earning links. */
export function SeoAuckland() {
  return <SeoPageShell content={auckland} />;
}
