import { SeoPageShell } from "../../components/SeoPageShell";
import { hawkesBay } from "../../content/seo/hawkes-bay";

/** /seo/hawkes-bay — 140/mo at KD 9 — a region, not a city: Napier and Hastings. */
export function SeoHawkesBay() {
  return <SeoPageShell content={hawkesBay} />;
}
