import { SeoPageShell } from "../../components/SeoPageShell";
import { wellington } from "../../content/seo/wellington";

/** /seo/wellington — 260/mo at KD 26 — the hardest of the small-city clusters. */
export function SeoWellington() {
  return <SeoPageShell content={wellington} />;
}
