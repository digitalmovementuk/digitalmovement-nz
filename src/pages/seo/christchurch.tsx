import { SeoPageShell } from "../../components/SeoPageShell";
import { christchurch } from "../../content/seo/christchurch";

/** /seo/christchurch — priority 1. 880/mo at KD 13, page one held by pages with almost no links. */
export function SeoChristchurch() {
  return <SeoPageShell content={christchurch} />;
}
