import { SeoPageShell } from "../../components/SeoPageShell";
import { hamilton } from "../../content/seo/hamilton";

/** /seo/hamilton — priority 1. KD 8, the lowest difficulty of any cluster in the plan. */
export function SeoHamilton() {
  return <SeoPageShell content={hamilton} />;
}
