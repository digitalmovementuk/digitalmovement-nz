import { SeoPageShell } from "../../components/SeoPageShell";
import { ecommerce } from "../../content/seo/ecommerce";

/** /seo/ecommerce — 320/mo at KD 6 with a 12.11 USD CPC — the anomaly in the dataset. */
export function SeoEcommerce() {
  return <SeoPageShell content={ecommerce} />;
}
